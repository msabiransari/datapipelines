{
  "templates": [
    {
      "schema_version": 1,
      "id": "sample_trips_monthly.sql",
      "engine": "freemarker",
      "dialect": "POSTGRES",
      "display_name": "Monthly trips by pickup zone",
      "description": "Reads the pre-aggregated trips_monthly rollup from the sample-trips Postgres datasource for the pipeline's start_date/end_date window. Expects DATE parameters named start_date and end_date; the shipped sample covers 2023-01-01..2024-12-31.",
      "imports": [],
      "body": "SELECT\n    m.month_start     AS month_start,\n    m.pu_location_id  AS pu_location_id,\n    m.trip_count      AS trip_count,\n    m.total_revenue   AS total_revenue,\n    m.total_tips      AS total_tips\nFROM trips_monthly m\nWHERE m.month_start BETWEEN :start_date AND :end_date",
      "is_library": false
    },
    {
      "schema_version": 1,
      "id": "sample_trips_daily.sql",
      "engine": "freemarker",
      "dialect": "POSTGRES",
      "display_name": "Daily trip counts and revenue",
      "description": "Reads the pre-aggregated trips_daily rollup from the sample-trips Postgres datasource for the pipeline's start_date/end_date window. Expects DATE parameters named start_date and end_date; the shipped sample covers 2023-01-01..2024-12-31.",
      "imports": [],
      "body": "SELECT\n    d.pickup_date       AS pickup_date,\n    d.trip_count        AS trip_count,\n    d.total_revenue     AS total_revenue,\n    d.total_distance_mi AS total_distance_mi\nFROM trips_daily d\nWHERE d.pickup_date BETWEEN :start_date AND :end_date",
      "is_library": false
    },
    {
      "schema_version": 1,
      "id": "sample_zones.sql",
      "engine": "freemarker",
      "dialect": "SQLITE",
      "display_name": "TLC taxi zones and boroughs",
      "description": "Reads the 265-row zone lookup from the sample-reference SQLite datasource. Takes no parameters. The two catch-all zones ('Unknown' and 'N/A') are excluded so a borough rollup has no bucket that is not a borough.",
      "imports": [],
      "body": "SELECT\n    z.location_id  AS location_id,\n    z.borough      AS borough,\n    z.zone         AS zone_name,\n    z.service_zone AS service_zone\nFROM zones z\nWHERE z.borough NOT IN ('Unknown', 'N/A')",
      "is_library": false
    },
    {
      "schema_version": 1,
      "id": "sample_calendar.sql",
      "engine": "freemarker",
      "dialect": "SQLITE",
      "display_name": "Weekend and US federal holiday calendar",
      "description": "Reads the generated day calendar from the sample-reference SQLite datasource for the pipeline's window. Expects DATE parameters named start_date and end_date. cal_date is ISO-8601 TEXT because SQLite has no DATE type; cast it after staging.",
      "imports": [],
      "body": "SELECT\n    c.cal_date     AS cal_date,\n    c.is_weekend   AS is_weekend,\n    c.is_holiday   AS is_holiday,\n    c.holiday_name AS holiday_name\nFROM calendar c\nWHERE c.cal_date BETWEEN :start_date AND :end_date",
      "is_library": false
    },
    {
      "schema_version": 1,
      "id": "sample_daily_rain.sql",
      "engine": "freemarker",
      "dialect": "MYSQL",
      "display_name": "Daily precipitation across the NYC stations",
      "description": "Averages the PRCP element across the five NYC-area GHCN stations, one row per day, from the sample-weather MySQL datasource. Expects DATE parameters named start_date and end_date. Values are millimetres.",
      "imports": [],
      "body": "SELECT\n    o.obs_date                AS obs_date,\n    ROUND(AVG(o.value), 2)    AS precip_mm,\n    ROUND(MAX(o.value), 2)    AS max_precip_mm\nFROM observations o\nWHERE o.element = 'PRCP'\n  AND o.obs_date BETWEEN :start_date AND :end_date\nGROUP BY o.obs_date",
      "is_library": false
    },
    {
      "schema_version": 1,
      "id": "sample_revenue_by_borough.sql",
      "engine": "freemarker",
      "dialect": "H2",
      "display_name": "Join staged trips to staged zones, roll up by borough",
      "description": "Runs in tempdb (H2) over stg_trips_monthly (Postgres) and stg_zones (SQLite). This is the cross-datasource join: neither engine can see the other's table, so both are staged first. Takes no parameters of its own.",
      "imports": [],
      "body": "SELECT\n    z.borough                                                AS borough,\n    SUM(t.trip_count)                                        AS trip_count,\n    ROUND(SUM(t.total_revenue), 2)                           AS total_revenue,\n    ROUND(SUM(t.total_tips), 2)                              AS total_tips,\n    ROUND(SUM(t.total_revenue) / SUM(t.trip_count), 2)       AS revenue_per_trip,\n    ROUND(100.0 * SUM(t.total_tips) / SUM(t.total_revenue), 2) AS tip_pct_of_revenue\nFROM stg_trips_monthly t\nJOIN stg_zones z ON z.location_id = t.pu_location_id\nGROUP BY z.borough\nORDER BY SUM(t.total_revenue) DESC",
      "is_library": false
    },
    {
      "schema_version": 1,
      "id": "sample_rain_vs_dry.sql",
      "engine": "freemarker",
      "dialect": "H2",
      "display_name": "Compare ridership on rainy versus dry days, by day type",
      "description": "Runs in tempdb (H2) over stg_trips_daily (Postgres), stg_daily_rain (MySQL) and stg_calendar (SQLite) \u2014 a three-datasource join. Expects a BIGDECIMAL parameter named rain_threshold_mm; a day at or above it counts as rainy. Days with no weather observation count as dry.",
      "imports": [],
      "body": "SELECT\n    CASE WHEN COALESCE(r.precip_mm, 0) >= :rain_threshold_mm THEN 'rainy' ELSE 'dry' END AS weather,\n    CASE WHEN c.is_holiday = 1 THEN 'holiday'\n         WHEN c.is_weekend = 1 THEN 'weekend'\n         ELSE 'workday' END                                   AS day_type,\n    COUNT(*)                                                  AS days,\n    SUM(t.trip_count)                                         AS trip_count,\n    ROUND(AVG(CAST(t.trip_count AS DECIMAL(18,2))), 1)        AS avg_trips_per_day,\n    ROUND(SUM(t.total_revenue), 2)                            AS total_revenue\nFROM stg_trips_daily t\nJOIN stg_calendar c ON CAST(c.cal_date AS DATE) = t.pickup_date\nLEFT JOIN stg_daily_rain r ON r.obs_date = t.pickup_date\nGROUP BY\n    CASE WHEN COALESCE(r.precip_mm, 0) >= :rain_threshold_mm THEN 'rainy' ELSE 'dry' END,\n    CASE WHEN c.is_holiday = 1 THEN 'holiday'\n         WHEN c.is_weekend = 1 THEN 'weekend'\n         ELSE 'workday' END\nORDER BY 1, 2",
      "is_library": false
    }
  ],
  "pipelines": [
    {
      "schema_version": 1,
      "name": "revenue_by_borough",
      "display_name": "Revenue by borough",
      "description": "Which NYC borough earns the most yellow-taxi revenue, and which tips best. Joins the Postgres trip rollup to the SQLite zone lookup \u2014 two different database engines, staged into tempdb and joined there. Run it as-is for the full 2024 calendar year.",
      "settings": {
        "tempdb": {
          "engine": "H2"
        }
      },
      "parameters": {
        "start_date": {
          "type": "DATE",
          "required": false,
          "default": "2024-01-01",
          "description": "Inclusive start of the reporting window. The sample covers 2023-01-01 to 2024-12-31."
        },
        "end_date": {
          "type": "DATE",
          "required": false,
          "default": "2024-12-31",
          "description": "Inclusive end of the reporting window. The sample covers 2023-01-01 to 2024-12-31."
        }
      },
      "nodes": [
        {
          "id": "stage_trips_monthly",
          "description": "Stage the monthly trip rollup from the sample-trips Postgres database.",
          "type": "DQL",
          "source": "sample-trips",
          "template": {
            "id": "sample_trips_monthly.sql",
            "version": 1
          },
          "output": {
            "target": "tempdb",
            "table": "stg_trips_monthly"
          },
          "depends_on": []
        },
        {
          "id": "stage_zones",
          "description": "Stage the taxi-zone lookup from the sample-reference SQLite database.",
          "type": "DQL",
          "source": "sample-reference",
          "template": {
            "id": "sample_zones.sql",
            "version": 1
          },
          "output": {
            "target": "tempdb",
            "table": "stg_zones"
          },
          "depends_on": []
        },
        {
          "id": "borough_revenue",
          "description": "Join the two staged tables in tempdb and roll up by borough.",
          "type": "DQL",
          "source": "tempdb",
          "template": {
            "id": "sample_revenue_by_borough.sql",
            "version": 1
          },
          "output": {
            "target": "caller"
          },
          "depends_on": [
            "stage_trips_monthly",
            "stage_zones"
          ]
        }
      ]
    },
    {
      "schema_version": 1,
      "name": "rainy_vs_dry_ridership",
      "display_name": "Rainy vs dry ridership",
      "description": "Do New Yorkers take more taxis when it rains? Joins daily trip counts (Postgres), daily precipitation from five NYC weather stations (MySQL) and a weekend/holiday calendar (SQLite) \u2014 three engines, one answer. Split by weather and by day type.",
      "settings": {
        "tempdb": {
          "engine": "H2"
        }
      },
      "parameters": {
        "start_date": {
          "type": "DATE",
          "required": false,
          "default": "2024-01-01",
          "description": "Inclusive start of the reporting window. The sample covers 2023-01-01 to 2024-12-31."
        },
        "end_date": {
          "type": "DATE",
          "required": false,
          "default": "2024-12-31",
          "description": "Inclusive end of the reporting window. The sample covers 2023-01-01 to 2024-12-31."
        },
        "rain_threshold_mm": {
          "type": "BIGDECIMAL",
          "precision": 6,
          "scale": 1,
          "required": false,
          "default": "2.5",
          "description": "Average daily precipitation, in millimetres, at or above which a day counts as rainy."
        }
      },
      "nodes": [
        {
          "id": "stage_trips_daily",
          "description": "Stage daily trip counts and revenue from the sample-trips Postgres database.",
          "type": "DQL",
          "source": "sample-trips",
          "template": {
            "id": "sample_trips_daily.sql",
            "version": 1
          },
          "output": {
            "target": "tempdb",
            "table": "stg_trips_daily"
          },
          "depends_on": []
        },
        {
          "id": "stage_daily_rain",
          "description": "Stage daily precipitation from the sample-weather MySQL database.",
          "type": "DQL",
          "source": "sample-weather",
          "template": {
            "id": "sample_daily_rain.sql",
            "version": 1
          },
          "output": {
            "target": "tempdb",
            "table": "stg_daily_rain"
          },
          "depends_on": []
        },
        {
          "id": "stage_calendar",
          "description": "Stage the weekend/holiday calendar from the sample-reference SQLite database.",
          "type": "DQL",
          "source": "sample-reference",
          "template": {
            "id": "sample_calendar.sql",
            "version": 1
          },
          "output": {
            "target": "tempdb",
            "table": "stg_calendar"
          },
          "depends_on": []
        },
        {
          "id": "rain_vs_dry",
          "description": "Join all three staged tables in tempdb and compare rainy against dry days.",
          "type": "DQL",
          "source": "tempdb",
          "template": {
            "id": "sample_rain_vs_dry.sql",
            "version": 1
          },
          "output": {
            "target": "caller"
          },
          "depends_on": [
            "stage_trips_daily",
            "stage_daily_rain",
            "stage_calendar"
          ]
        }
      ]
    }
  ]
}
